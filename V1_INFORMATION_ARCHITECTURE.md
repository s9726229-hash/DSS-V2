# V1 資訊架構與白話 UI 設計

## 目標

每個頁面只回答一個使用者問題，降低導覽重疊感，但不改變任何資料或計算行為。

## 側欄

| V0 名稱 | V1 名稱 | 要回答的使用者問題 |
| --- | --- | --- |
| 今日 DSS | 今日總覽 | 我的持股今天有什麼變化？ |
| 技術分析 | 完整分析 | 這檔持股的狀態，是由哪些原始資料與計算得出？ |
| 歷史交易研究 | 歷史研究 | 我的過去交易資料支持哪些觀察？ |
| 目前規則 | 目前規則 | 現在套用了哪些門檻？ |
| 資料中心 | 資料中心 | 我要怎麼匯入、同步與保護資料？ |
| 判讀說明 | 系統怎麼算 | 資料如何變成我看到的判讀？ |
| 設定 | 隱藏 | 在具備實際設定功能前不顯示。 |

`完整分析` 保留獨立頁面。它是精簡的 `今日總覽` 卡片之查核／細節頁，而不是第二個總覽頁。

## 系統怎麼算

將目前以名詞為主的判讀說明，改成流程優先的頁面：

1. 匯入：庫存與交易歷史留在這台電腦。
2. 同步：中介服務向 FinMind 取得允許使用的市場資料，瀏覽器不會取得 API Key。
3. 整理：價格會在資料可用時排除配息與分割造成的帳面跳動；資料不足時，畫面會直接說資料不足。
4. 計算今天：技術面、法人流向與融資三類資料各自計算，不混成一個分數。
5. 研究歷史：系統把舊交易分成較早的資料與較後的資料，反覆檢查候選門檻是否仍成立；結果是證據，不是預測。
6. 套用規則：只有使用者確認的目前規則，才會把候選門檻變成目前卡片的標籤。

頁面採三段漸進結構：

- `今天的卡片怎麼來`：目前資料的流程，並連結到詳細名詞。
- `歷史研究怎麼來`：研究流程，包含目前的研究計算流程。
- `系統不做什麼`：沒有盤中價格、不產生自動交易指令、不把 API Key 放進瀏覽器。

技術名詞保留在對應流程下方，以可展開的白話說明呈現，而不是一進頁面就先看到一長串術語。

## 移動計算流程

- 將技術分析的計算流程分頁移至 `系統怎麼算 / 今天的卡片怎麼來`。
- 將歷史交易研究的計算流程分頁移至 `系統怎麼算 / 歷史研究怎麼來`。
- `完整分析` 保留原始計算、權息調整提示與逐檔面板。

## 白話頁面結構

每個操作頁面遵循以下層次：

1. 一句日常語言的頁面目的。
2. 只有在需要操作時才放一個清楚的主要動作。
3. 使用者理解目前狀態或下一步所需的少量事實。
4. 原始數字、名詞定義與流程細節放在分頁、明細面板或展開內容。

目標頁首文案：

- 今日總覽：`今天的持股狀態，以及值得再查看的變化。`
- 完整分析：`逐檔查看原始數字、趨勢與計算依據。`
- 歷史研究：`用過去交易資料檢查哪些條件值得繼續追蹤。`
- 目前規則：`目前套用在個股與 ETF 的判讀門檻。`
- 資料中心：`匯入、同步與備份你的本機資料。`
- 系統怎麼算：`了解資料如何一路變成今天看到的判讀。`

## 驗收條件

- 側欄不再出現沒有完成的暫時頁面。
- 使用者看完「今日總覽」與「完整分析」第一句，就能分辨兩頁用途。
- 兩個計算流程都能從系統怎麼算取得，且不再占用操作頁面的分頁。
- 判讀說明先解釋資料到標籤的流程，再介紹專有名詞。
- 既有資料、計算、目前規則與市場同步行為不變。
- 既有自動測試通過；只在移動內容或更名需要時新增／調整針對導覽與頁面文字的測試。
