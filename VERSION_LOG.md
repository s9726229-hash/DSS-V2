# DSS V2 版本紀錄

## V2.0 — 情境化歷史研究

狀態：本機實作與驗證完成；尚未推送、合併、部署或發布。

預定範圍：

- 將建立部位、加碼、再進場拆分為獨立研究樣本與歷史研究分頁。
- 加碼研究新增加碼前相對均價指標。
- Profile 依決策情境分開，既有 V1 通用 Profile 不自動搬移。

完成範圍：

- 保留交易方式 metadata；相同 CSV 可補齊舊紀錄而不重複匯入。
- `普通`與`盤中零股`現股交易可依持倉狀態納入建立、加碼或再進場；其他零股方式、定期定額、未知方式、現沖與非現股維持排除。
- 歷史研究的摘要、樣本名稱與空資料提示會隨目前情境切換。
- 建立共用可靠現股帳本，統一供研究、交易歷史與今日情境使用；包含同日合併、部分賣出、清零再進場、拆股與穩定排除原因。
- 建立部位、加碼、再進場的研究報告與樣本完全隔離；個股與 ETF 也分開計算。
- A1 相對均價使用加碼實際成交均價與加碼前移動平均成本，均不含手續費與交易稅。
- R1 使用事件日還原收盤價至個股第 20／ETF 第 60 個交易日還原收盤價；不完整觀察窗保留但不納入統計。
- 三情境 Profile 分開保存，候選需預覽與明確確認；V1 通用 Profile 不搬移。
- 今日總覽依可靠帳本選擇 Profile；預計加碼價為不持久化的畫面試算，並標示不是交易建議。
- 今日總覽優先以最新原始市場收盤價估算現值、未實現損益與報酬率；缺少市場價才退回券商快照，並分別標示資料來源與日期。狀態列的同步日期改稱「最後同步」，不再冒充交易日期。

驗證：

- 前端 46 個測試檔、548 項測試全部通過。
- Worker 52 項測試全部通過。
- TypeScript 型別檢查通過。
- 使用工作區 Node 24.14.0 完成正式建置；112 個模組並產出完整 gzip 摘要。

實際資料彙總（不含個人明細）：

- 571 筆交易，日期範圍 2025-01-02 至 2026-07-29。
- 2026 年可靠研究事件：建立部位 25、加碼 79、再進場 19；其中觀察窗完整分別為 22、41、19。
- 排除原因可能重疊：期初部位不明 6、同日反向 2、定期定額 48、現沖 70；交易方式不明、非現股部位與拆股資料缺漏均為 0。
- 重匯來源 CSV 的 554 筆交易全數已存在，新增與補齊皆為 0；本機交易總數維持 571，來源檔未涵蓋的較早 17 筆不推測交易種類。
- 歷史資料回補完成；建立、加碼與再進場均已有可分析樣本，但各候選仍依實際證據門檻顯示資料不足或初步觀察，不自動套用 Profile。
- 三情境報告與 Profile 命名空間保持隔離；三個 V2 Profile 目前皆為空白。

## V1.8 — 規則表四欄對齊

狀態：完成。

範圍：

- 規則表改為固定「指標／下界／一般／上界」四欄，使個股與 ETF 的欄位和數值對齊。
- 下界與上界保留可編輯門檻；中間欄只顯示各指標的合理區、中性等判讀名稱。
- 複核小圖示改與門檻標籤同列，避免與文字重疊。

範圍外：

- 不修改門檻、計算、研究、Profile、Worker 或資料保存規則。

驗證：

- 目前規則整合測試、型別檢查、完整自動測試與正式建置通過。

## V1.7 — 規則門檻的判讀軸

狀態：完成。

範圍：

- 個股與 ETF 的規則表改用固定欄寬，表頭維持「指標／下界／上界」。
- 指標欄只保留名稱；回檔／合理區／偏熱、賣超／中性／買超、減少／中性／增加改排在兩個門檻值之間。

範圍外：

- 不修改門檻、計算、研究、Profile、Worker 或資料保存規則。

驗證：

- 目前規則整合測試、型別檢查、完整自動測試與正式建置通過。

## V1.6 — 規則來源的圖例化

狀態：完成。

範圍：

- 將目前規則的證據與套用狀態集中為表格上方圖例。
- 橘框保留為需要複核的視覺提示；格內移除重複的來源文字與日期，只在右上角保留小圖示。

範圍外：

- 不修改門檻、計算、研究、Profile、Worker 或資料保存規則。

驗證：

- 目前規則整合測試、型別檢查、完整自動測試與正式建置通過。

## V1.5 — 目前規則的複核提示

狀態：完成。

範圍：

- 目前規則的說明文收斂為單行短句，移除不必要的換行。
- 門檻輸入值固定以小數第 2 位顯示。
- 將「資料不足仍套用」與「證據不足仍套用」集中在頁首橘色複核提示，並以淡橘框標示對應門檻格。

範圍外：

- 不新增、推測或自動套用市場慣例參數；資料不足的門檻仍維持未設定。
- 不修改技術、籌碼、研究、Profile、Worker 或資料保存的計算規則。

驗證：

- 目前規則整合測試、型別檢查、完整自動測試與正式建置通過。

## V1.4 — 完整分析閱讀層級

狀態：完成。

範圍：

- 完整分析的「已還原權息與分割」預設收合；沒有事件時不顯示。
- 技術面總覽與籌碼明細改為上下全寬閱讀，避免兩欄造成留白與資訊擁擠。
- 三個籌碼趨勢圖各只保留圖表本身，將「最近 20 個交易日、虛線為前 5 日平均」集中說明一次。

範圍外：

- 不修改技術、籌碼、研究或 Profile 的計算規則與門檻。

驗證：

- 完整分析元件測試、型別檢查、完整自動測試與正式建置通過。

## V1.3 — 今日判讀規則與卡片可讀性

狀態：完成。

範圍：

- 側邊欄「系統怎麼算」改為「判讀說明」。
- 判讀說明頁最前段明列資料門檻、月線／季線觸發條件與 Profile 紅綠灰區間規則。
- 今日總覽持股卡固定四個指標同列，未設規則集中到今日判讀列，並拉開判讀列與指標卡的視覺層級。

範圍外：

- 不修改技術、籌碼、研究或 Profile 的計算規則與門檻。

驗證：

- 導覽與判讀說明的前端測試、型別檢查、完整自動測試與正式建置通過。

## V1.2 — 今日判讀與完整分析分工

狀態：完成。

預定範圍：

- 今日總覽保留小卡與右側彈出檢視，收斂為套用 Profile 後的每日判讀。
- 右側彈窗與完整分析共用同一份完整分析內容。
- 籌碼趨勢圖只顯示最近 20 個交易日。
- 系統怎麼算改為較容易閱讀的中文流程卡。
- 持股小卡改為「報酬率／庫存現價／持有天數」、「今日判讀」與四個 Profile 指標三層；指標下界綠、中間灰、上界紅。
- 未設規則改為集中顯示於今日判讀列，四個指標固定同列，窄螢幕才改為兩列。

範圍外：

- 不修改技術、籌碼、研究或 Profile 的計算規則與門檻。
- 不變更市場資料、Worker、資料保存方式或個人財務資料。

驗證：

- 新增近 20 個交易日截取測試，先確認缺少實作時失敗，再通過。
- 右側彈窗與完整分析共用完整分析內容的元件與測試通過。
- 今日總覽、系統流程卡、完整分析的相關前端測試與型別檢查通過。

## V1.1 — 本機 Worker CORS 修正

狀態：完成。

範圍：

- 允許本機 Vite 預覽的 `http://127.0.0.1:5174` 呼叫 FinMind Worker。

範圍外：

- 不修改 FinMind Secret、資料集、資料計算、資料保存或個股／ETF 規則。

驗證：

- Worker CORS 測試先失敗，再於加入白名單後通過。
- `node --test worker/finmind-gateway.test.mjs worker/finmind-usage.test.mjs` 通過（52 項）。
- 已部署正式 Worker，並以 `Origin: http://127.0.0.1:5174` 確認回傳正確的 CORS 標頭。
- 瀏覽器端到端同步完成，未再出現同步失敗或主控台錯誤。

## V1 — 資訊架構與白話 UI

狀態：完成。

範圍：

- 依使用任務重整側欄名稱與順序。
- 保留完整分析，不與今日總覽合併。
- 將目前的判讀說明改為白話的「系統怎麼算」。
- 將計算流程說明移出操作頁面，集中到系統怎麼算。
- 簡化每頁開頭與漸進揭露方式。

範圍外：

- 不修改計算公式、研究邏輯、目前規則的行為、中介服務、資料保存方式或私人資料。

實作後驗證：

- 執行既有自動測試、型別檢查與正式版本製作。
- 在瀏覽器確認每個側欄目的地、今日 DSS 的詳細頁、系統怎麼算、歷史研究與規則標記。

已完成：

- 側欄改為六個完成頁面：今日總覽、完整分析、系統怎麼算、歷史研究、目前規則、資料中心；設定暫時頁面已隱藏。
- 六個頁面首段改為白話用途說明。
- `npx.cmd vitest run src/app/AppShell.test.tsx src/features/analysis/AnalysisPage.test.tsx src/features/research/ResearchPage.test.tsx` 通過（45 項）。
- 系統怎麼算改為資料總流程、今天卡片流程、歷史研究流程與系統限制四段；技術名詞預設收合。
- `npx.cmd vitest run src/features/guide/GuidePage.test.tsx` 通過（11 項）。
- 完整分析與歷史研究不再顯示計算流程分頁；兩張流程圖只在系統怎麼算呈現。
- `npx.cmd vitest run src/features/analysis/AnalysisPage.test.tsx src/features/research/ResearchPage.test.tsx` 通過（25 項）。
- `npm.cmd run typecheck` 通過。
- `npm.cmd test` 通過：前端 492 項、中介服務 52 項。
- `npm.cmd run build` 通過；本次沒有重現 V0 曾遇到的本機 Node 24 建置異常。
- 瀏覽器驗收通過：六個導覽目的地、兩張流程圖、操作頁無流程分頁，且沒有主控台錯誤。

## V0 — 已驗證可運作的基線

狀態：完成基線。

關鍵證據：

- 型別檢查通過。
- 自動測試通過：共 545 項（前端 493、中介服務 52）。
- 使用真實本機資料完成整合測試：匯入、歷史回補、目前同步、研究、個股規則套用、今日 DSS 與技術分析皆成功。

已知限制：

- 本機以 Node 24 製作正式版本時，曾在前端檔案整理完成後以 `0xC0000409` 結束；自動驗證環境仍固定使用 Node 22。
