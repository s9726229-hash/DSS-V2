# DSS V2 專案交接文件

> 變更專案前請先閱讀本文件；它是供人員與 AI 交接使用的長期背景資料。
> 不得在此記錄 API Key、原始庫存、交易明細、備份檔或其他個人財務資料。

## 目前基線：V0

V0 是截至 2026-08-02 已驗證可運作的基準版本。它是一個「資料只留在這台電腦」的台股判讀工具，提供研究觀察，不提供買賣指令。

### 已驗證的 V0 流程

1. 匯入券商交易歷史與庫存快照。
2. 透過中介服務向 FinMind 取得允許使用的市場資料，瀏覽器不會接觸 API Key。
3. 將來源資料保存於瀏覽器本機資料庫。
4. 計算技術面、法人流向與融資指標。
5. 回補歷史研究資料，並用「隨資料量逐步增加的回測」檢查候選門檻是否可靠。
6. 視使用者決定，將已確認的規則門檻套用到目前卡片。
7. 在今日 DSS 與技術分析檢視結果。

已使用私人本機資料完成實測（不得將資料寫入版本紀錄）：匯入、歷史回補、目前市場同步、研究、規則套用、今日 DSS 與技術分析皆完成，且畫面沒有執行錯誤。

### V0 資訊架構

- 今日 DSS：目前持股與觀察清單卡片。
- 技術分析：每一持股的技術／籌碼原始細節，以及「這個數字怎麼算」的分頁。
- 判讀說明：名詞與計算規則說明。
- 歷史交易研究：歷史交易研究、候選門檻、交易歷史、研究計算流程與搜尋紀錄。
- 目前規則：目前個股／ETF 門檻。
- 資料中心：匯入、本機存量、備份與還原。
- 設定：目前只有暫時畫面，尚非完成的功能。

### 保護原則

- 個股與 ETF 的研究／規則門檻必須完全分開。
- 規則分類是使用者控制的本機設定，不是投資建議。
- 證據不足的候選必須經使用者明確確認才能套用，且必須保留證據標記。
- 不得將 FinMind 憑證放進前端程式、瀏覽器儲存空間、版本紀錄、匯出檔或文件。
- 維持「資料只留在這台電腦」與備份安全的原則。

### V0 已知限制

- 盤中價格刻意未啟用；所有計算都採用最近一個收盤交易日。
- FinMind 還原價格資料的可用性可能取決於帳號方案。
- 本機以 Node 24 製作正式版本時，曾在前端檔案整理完成後以 `0xC0000409` 結束；自動驗證環境使用 Node 22。此問題尚未在 V0 解決。

## 目前版本：V2.0（本機實作已驗證，尚未發布）

V1.1 修正本機 Vite 預覽使用 `http://127.0.0.1:5174` 時的 Worker CORS 白名單；只影響瀏覽器是否能讀取 Worker 回應，不改變任何 FinMind Secret、資料與計算邏輯。

V1.2 將今日總覽收斂為 Profile 的每日判讀小卡；右側詳情與完整分析共用同一份技術與籌碼內容。完整分析的籌碼圖只保留最近 20 個交易日；系統怎麼算使用可回應式的中文流程卡。計算、研究、Profile、Worker 與資料保存規則均未修改。

V1.3 將側邊欄名稱改為「判讀說明」，並將今日判讀的資料門檻、技術條件與 Profile 紅綠灰區間規則集中在頁首；持股卡的四個指標固定同列並與今日判讀分區呈現。

V1.4 將完整分析改為技術面總覽後接全寬籌碼明細；已還原權息與分割預設收合，三張籌碼趨勢圖的共同閱讀方式只說明一次。計算、研究、Profile、Worker 與資料保存規則均未修改。

V1.5 將目前規則的說明收斂為短句、門檻統一顯示至小數第 2 位，並集中標示資料不足或證據不足仍套用的門檻。沒有新增或自動套用外部參考參數，計算與資料規則均未修改。

V1.6 將目前規則的證據與套用狀態集中成表格上方圖例；需要複核的門檻保留橘框，格內改以小圖示取代重複文字與日期。計算與資料規則均未修改。

V1.7 將目前規則的個股與 ETF 表格統一成固定欄寬，維持「指標／下界／上界」表頭，並將各指標的三種判讀名稱排在兩個門檻值之間。計算與資料規則均未修改。

V1.8 將目前規則表改為「指標／下界／一般／上界」四欄；個股與 ETF 對齊，兩側仍可編輯，中間只呈現判讀名稱。複核圖示改與門檻標籤同列。計算與資料規則均未修改。

V2.0 已在隔離 worktree 完成建立部位、加碼、再進場三種情境的本機實作；尚未推送、合併、部署或發布。完整規格見 `V2_CURRENT_REQUIREMENTS.md`。

### V2.0 已完成

- 券商「交易種類」另存為交易方式；相同 CSV 可補齊舊資料而不建立重複交易。
- 研究、交易歷史與今日總覽共用可靠現股帳本；帳本處理同日合併、部分賣出、清零再進場、拆股、期初不明、同日反向、定期定額、未知交易方式與非現股排除。
- A1 相對均價使用實際加碼成交均價與加碼前移動平均成本，兩者都不含手續費與交易稅。
- R1 使用事件日還原收盤價至個股第 20／ETF 第 60 個交易日還原收盤價；未完成觀察窗不納入統計。
- 三情境的研究報告、候選與 Profile 分開保存；加碼情境獨有相對均價。候選仍需預覽與明確確認，V1 通用 Profile 不自動搬移。
- 今日總覽依可靠帳本選擇情境。預計加碼價只存在 React 畫面狀態，使用可靠帳本均價試算，重新進入頁面即清除，且明示不是交易建議。

### V2.0 驗證證據（2026-08-08）

- 前端完整回歸：46 個測試檔、540 項測試全部通過。
- Worker：52 項測試全部通過。
- TypeScript 型別檢查通過。
- 正式建置使用工作區 Node 24.14.0 完成：112 個模組，產出 HTML、CSS、JavaScript 與 gzip 摘要。

### 本機實際資料彙總與限制

- 讀到 571 筆交易，日期範圍為 2025-01-02 至 2026-07-29。
- 2026 年情境事件：建立部位 25（個股 21、ETF 4）、加碼 97（個股 50、ETF 47）、再進場 21（個股 17、ETF 4）。
- 目前三情境可納入研究皆為 0，故觀察窗未完成數也皆為 0；這代表尚無可靠研究樣本，不代表所有觀察窗均已完成。
- 排除原因可能重疊：期初部位不明 6、同日反向交易 2、交易方式不明 446、現沖 70、拆股資料缺漏 446；定期定額與非現股部位本次皆為 0。
- 66 檔尚缺已驗證的拆股覆蓋。舊資料需重新匯入相同 CSV 以補齊交易方式，並完成拆股覆蓋回補後，才可產生可靠研究樣本。
- 三份研究報告的情境與指標命名空間已驗證隔離；三個 V2 Profile 目前均為空白，且各自使用獨立命名空間。
- 上述驗證只讀取並回報彙總；未輸出個股、名稱、持股、價格、數量或單筆交易日期。

## V1：資訊架構與白話 UI

狀態：完成（2026-08-03）。V1 只調整了導覽、頁面結構與文案；DSS、研究、目前規則、中介服務、資料保存方式與市場資料計算邏輯均未修改。

目前導覽為：今日總覽、完整分析、系統怎麼算、歷史研究、目前規則、資料中心。系統怎麼算集中兩張計算流程圖；完整分析與歷史研究不再重複顯示流程分頁。

完整設計見 [V1_INFORMATION_ARCHITECTURE.md](V1_INFORMATION_ARCHITECTURE.md)。

## 版本紀錄規則

實作前，先將預定範圍寫入 `VERSION_LOG.md`；實作後，再補上驗證證據與仍存限制。內容應保持客觀、精簡，且不得包含個人資料。
